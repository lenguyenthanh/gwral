# Lichess World Record attempt tool

Run and compile using [scala-cli](https://scala-cli.virtuslab.org)

```
MONGO_URI=mongodb://localhost:27017 MONGO_DATABASE=lichess scala-cli run WorldRecord.scala
```
